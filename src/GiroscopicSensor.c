#include "../include/GiroscopicSensor.h"

int main(int argc, char **argv){

    int grados_desfase = 0;

    while(1){
        if(grados_desfase != 0) printf("Envia mensaje al control");
        sleep(1);
    }

    return 0;
}